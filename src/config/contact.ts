/**
 * Centralized support contact details for buyer-facing CTAs
 * (Sold-tab "Request Availability", error fallbacks, etc.).
 *
 * TODO: Replace with the real Flux dealer support number before launch.
 * Format: country code + number, digits only (no '+', spaces, or dashes).
 */
export const FLUX_SUPPORT_WHATSAPP = '254700000000';
